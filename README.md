# Calculator
## Реализовать калькулятор в Windows Forms. Калькулятор должен:

  Pаботать со скобочными выражениями, например, ((...)*(...) - (...)) 

  Работать с выражениями "- (-1)"
  
  Поддерживать операции: - +, -, *, / (не целочисленное)

